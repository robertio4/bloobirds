/**
 * For loading via <script> tag and for people who just want to `import IntegraionApp from '@integration-app/sdk'`
 */
import { IntegrationAppClient } from './client'

module.exports = new IntegrationAppClient({})
