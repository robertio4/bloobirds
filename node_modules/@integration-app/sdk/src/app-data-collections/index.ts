import { HttpRequestSpec } from '../http-requests'
import { User } from '../users'

export interface AppDataCollection {
  id: string

  key: string

  name: string

  fieldsSchema?: any

  specRequest?: HttpRequestSpec

  findRequest?: HttpRequestSpec

  findOneRequest?: HttpRequestSpec

  createRequest?: HttpRequestSpec

  updateRequest?: HttpRequestSpec

  deleteRequest?: HttpRequestSpec

  subscribeRequest?: HttpRequestSpec

  updateSubscriptionRequest?: HttpRequestSpec

  unsubscribeRequest?: HttpRequestSpec
}

export interface AppDataCollectionInstance {
  id: string

  userId: string

  user?: User

  appDataCollectionId: string

  appDataCollection?: AppDataCollection

  instanceKey?: string

  parameters?: any

  specRequest?: HttpRequestSpec

  specResponse?: any
}
