import { Self } from '.'
import { IntegrationAppClient } from '../client'

export class SelfAccessor {
  constructor(private client: IntegrationAppClient) {}

  async get(): Promise<Self> {
    return this.client.get('self')
  }
}
