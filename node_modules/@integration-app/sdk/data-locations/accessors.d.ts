import { IntegrationAppClient } from '../client';
import { DataLocationSelector } from './api';
export declare class DataLocationAccessor {
    private client;
    private selector;
    constructor(client: IntegrationAppClient, selector: DataLocationSelector);
    get(): Promise<any>;
    list(): Promise<any>;
    private getPath;
}
