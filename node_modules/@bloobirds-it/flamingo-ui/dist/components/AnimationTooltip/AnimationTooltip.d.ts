import React from "react";
import { BasePlacement, VariationPlacement } from "@popperjs/core";
import { ColorType } from "../../typings";
export type Props = {
    arrow?: boolean;
    text?: React.ReactNode;
    color?: ColorType;
    id?: number;
    horizontalAnimation?: boolean;
    verticalAnimation?: boolean;
    width: number | string;
    left: number | string;
    top: number | string;
    position?: BasePlacement | VariationPlacement;
    element?: Element;
};
declare const AnimationTooltip: {
    ({ arrow, text, color, id, horizontalAnimation, verticalAnimation, width, left, top, position, element }: Props): React.JSX.Element;
    displayName: string;
    defaultProps: {
        element: HTMLElement;
        arrow: boolean;
        horizontalAnimation: boolean;
        verticalAnimation: boolean;
        width: string;
        left: number;
        top: number;
    };
};
export default AnimationTooltip;
