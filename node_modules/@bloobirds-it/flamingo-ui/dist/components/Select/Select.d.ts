import * as React from "react";
import { BaseSelectProps } from "../BaseSelect/BaseSelect";
export interface SelectProps extends Omit<BaseSelectProps, "focused" | "filled" | "onBlur"> {
    onChange?: (value: any) => void;
    autocomplete?: boolean;
    renderDisplayValue?: (value: any) => string;
}
declare const Select: {
    (Props: SelectProps): React.JSX.Element;
    defaultProps: {
        size: string;
        autocomplete: boolean;
        borderless: boolean;
        children: any[];
    };
};
export default Select;
