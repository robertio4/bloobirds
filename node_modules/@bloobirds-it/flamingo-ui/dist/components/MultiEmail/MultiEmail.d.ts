import * as React from "react";
import { BaseInputProps } from "../BaseInput/BaseInput";
export interface MultiEmailProps extends BaseInputProps {
    name?: string;
    value?: string[];
    defaultValue?: string[];
    onChange?: (emails: string[]) => void;
}
declare const MultiEmail: {
    (Props: MultiEmailProps): React.JSX.Element;
    defaultProps: {
        defaultValue: any[];
    };
};
export default MultiEmail;
