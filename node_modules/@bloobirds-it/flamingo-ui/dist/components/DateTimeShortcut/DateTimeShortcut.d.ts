import * as React from "react";
interface DateTimeShortcutProps {
    text: string;
    date: Date;
    timezone: string;
    onClick: (date: Date) => void | Promise<void>;
    format?: string;
}
declare const DateTimeShortcut: ({ text, timezone, date, onClick, format }: DateTimeShortcutProps) => React.JSX.Element;
export default DateTimeShortcut;
