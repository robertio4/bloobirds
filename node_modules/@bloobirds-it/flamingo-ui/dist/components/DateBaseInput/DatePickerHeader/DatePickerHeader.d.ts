import * as React from "react";
interface DatePickerHeader {
    children: any;
    onNext: () => void;
    onBack: () => void;
}
declare const DatePickerHeader: ({ children, onNext, onBack }: DatePickerHeader) => React.JSX.Element;
export default DatePickerHeader;
