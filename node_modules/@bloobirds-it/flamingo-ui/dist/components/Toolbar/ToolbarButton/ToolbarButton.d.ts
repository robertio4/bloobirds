import React, { HTMLProps } from "react";
import { ColorType, IconType } from "../../../typings";
export interface ToolbarButtonProps {
    icon: IconType;
    active?: boolean;
    onClick?: HTMLProps<HTMLButtonElement>["onClick"];
    onMouseDown?: HTMLProps<HTMLButtonElement>["onMouseDown"];
    color: ColorType;
    isDropdown?: boolean;
}
declare function ToolbarButton(props: ToolbarButtonProps): React.JSX.Element;
declare namespace ToolbarButton {
    var defaultProps: {
        color: string;
    };
}
export default ToolbarButton;
