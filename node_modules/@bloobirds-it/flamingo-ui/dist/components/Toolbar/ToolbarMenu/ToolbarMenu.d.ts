import React from "react";
import { IconType } from "../../../typings";
export interface ToolbarButtonProps {
    children: any;
    icon: IconType;
    width: number;
    visible: boolean;
    onClick: () => void;
}
declare const ToolbarMenu: React.ForwardRefExoticComponent<ToolbarButtonProps & React.RefAttributes<HTMLDivElement>>;
export default ToolbarMenu;
