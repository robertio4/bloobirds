import React from "react";
import { ColorType } from "../../typings";
export interface ToolbarProps {
    children: any;
    className?: string;
    disabled?: boolean;
    backgroundColor?: ColorType;
}
declare const Toolbar: (props: ToolbarProps) => React.JSX.Element;
export default Toolbar;
