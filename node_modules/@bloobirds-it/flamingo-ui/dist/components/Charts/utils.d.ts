/**
 * Some util functions to control visuals of charts according to design system.
 */
export declare const colorScale: string[];
/**
 * Returns an index of the scale based on a count (number of bars in a bar chart).
 */
export declare const scaleIndexFromCategoriesCount: (count: number) => 1 | 2 | 3 | 0;
export declare function scaledHeight(count: number): number;
export declare function scaledFontSize(count: number): number;
export declare function scaledMaxLines(count: number): number;
