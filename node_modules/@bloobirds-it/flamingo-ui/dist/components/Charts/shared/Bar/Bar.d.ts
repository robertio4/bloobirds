import * as React from "react";
export declare const DEFAULT_BAR_COLOR: string;
export interface BarProps {
    isHovered?: boolean;
    ignorePointerEvents?: boolean;
    showLines?: boolean;
    x: number;
    y: number;
    barIndex: number;
    originalHeight: number;
    height: number | ((args: any) => number);
    width: number;
    radius: number;
    payload: Record<string, any>;
    rounded: boolean;
    dataKey: string;
    groupKey: string;
    index?: number;
    isLast: boolean;
    fill: string | ((dataKey: string, index: number) => string);
    opacity?: string;
    clickable?: boolean;
    outline?: boolean;
}
export declare const Bar: (props: BarProps) => React.JSX.Element;
