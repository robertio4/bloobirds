import React from "react";
interface ChartTooltipProps {
    getItemColor: (item: any, index: number) => string;
    getItemName: (dataKey: string, value: any, index: number) => string;
    getTitle?: (label: string) => string;
    sortData?: boolean;
    unit?: string;
    renderLabel?: (value: any) => string;
}
export declare const ChartTooltip: {
    (Props: ChartTooltipProps): React.JSX.Element;
    defaultProps: {
        getTitle: (label: string) => string;
    };
    displayName: string;
};
export {};
