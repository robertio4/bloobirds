import React from "react";
interface LabelsProps {
    x?: number;
    y?: number;
    width?: number;
    height?: number;
    payload?: Record<string, any>;
    points: Array<Record<string, any>>;
    getGroupLabel: (groupKey: string) => string;
    dataKeys: Array<string>;
    containerDimensions?: {
        width: number;
        height: number;
    };
    unit?: string;
    renderLabel?: (value: any) => string;
    hideAmount?: boolean;
}
declare const Labels: (Props: LabelsProps) => React.JSX.Element;
export default Labels;
