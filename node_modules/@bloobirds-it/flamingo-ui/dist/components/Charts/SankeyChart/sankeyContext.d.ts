import React from "react";
import { SankeyLink, SankeyNode } from "recharts/types/util/types";
export interface Node extends SankeyNode {
    name: string;
    depth: number;
    color?: string;
}
export interface Link extends SankeyLink {
    days?: number;
}
export interface SankeyData {
    nodes: SankeyNode[];
    links: SankeyLink[];
}
export interface TooltipData {
    items?: number;
    sourcePayload: SankeyNode;
    targetPayload: SankeyNode;
    extra: {
        [key: string]: any;
    };
}
interface SankeyState {
    data: SankeyData;
    updateData: (data: SankeyData) => void;
    total: number;
    updateTotal: (total: number) => void;
    mouse: {
        x: number;
        y: number;
        clientX: number;
        clientY: number;
        screenX: number;
        screenY: number;
    };
    mouseRef: React.MutableRefObject<any>;
    isTooltipVisible: boolean;
    updateTooltipVisible: (visible: boolean) => void;
    tooltipData: TooltipData;
    setTooltipData: (data: TooltipData) => void;
    maxWidth?: number;
    setMaxWidth: (width: number) => void;
}
export declare const useSankey: () => SankeyState;
export declare const withSankeyContext: (Component: any) => (props: any) => React.JSX.Element;
export {};
