import React from "react";
import { ToastAction } from "../../../hooks";
export interface ActionProps {
    duration: number;
    message: string;
    actions: Array<ToastAction>;
    onDismiss: () => void;
    onFinish: () => void;
}
declare const Action: ({ duration, message, actions, onDismiss, onFinish }: ActionProps) => React.JSX.Element;
export default Action;
