import React, { MouseEvent } from "react";
import { ColorType, IconType } from "../../../typings";
export interface ReminderProps {
    message: string;
    subtitle: string;
    icon: IconType;
    iconColor: ColorType;
    onClick?: (e: MouseEvent<HTMLElement>) => void;
    onDismiss: (e: any) => void;
    sound: string;
}
declare const Reminder: ({ message, subtitle, icon, onClick, iconColor, onDismiss, sound }: ReminderProps) => React.JSX.Element;
export default Reminder;
