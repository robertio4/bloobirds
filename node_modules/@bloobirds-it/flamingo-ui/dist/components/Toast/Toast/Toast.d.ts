import React from "react";
import { ToastType } from "../../../hooks";
export interface ToastProps {
    type: ToastType;
    message: string;
    onDismiss: () => void;
}
declare const Toast: ({ type, message, onDismiss }: ToastProps) => React.JSX.Element;
export default Toast;
