import * as React from "react";
import { CSSProperties } from "react";
type Contact = {
    email: string;
    color: string;
    fullName: string;
    shortName: string;
};
export interface EmailRecipientSelectProps {
    name?: string;
    id: string;
    value?: Array<Contact>;
    defaultValue?: Array<Contact>;
    contacts?: Array<Contact>;
    className?: string;
    style?: CSSProperties;
    selectFirst?: boolean;
    onChange?: (value: Array<Contact>) => void;
}
declare const RecipientInput: (props: EmailRecipientSelectProps) => React.JSX.Element;
export default RecipientInput;
