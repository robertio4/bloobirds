import * as React from "react";
export declare enum SlideDirection {
    Right = -1,
    Left = 1
}
export interface CarouselProps {
    children: any;
    activeSlide?: number;
    show?: number;
    onChange?: (index: number) => void;
}
declare const Carousel: {
    (Props: CarouselProps): React.JSX.Element;
    defaultProps: {
        show: number;
    };
};
export default Carousel;
