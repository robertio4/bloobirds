import * as React from "react";
import { CountryCode } from "../../typings";
export interface FlagProps {
    code: CountryCode;
    size?: number;
}
declare const Flag: {
    ({ code, size }: FlagProps): React.JSX.Element;
    defaultProps: {
        size: number;
    };
};
export default Flag;
