import * as React from "react";
import { SyntheticEvent } from "react";
import { ColorType } from "../../typings";
export interface CheckboxProps {
    checked?: boolean;
    children?: any;
    dataTest?: string;
    defaultChecked?: boolean;
    disabled?: boolean;
    expand?: boolean;
    onClick?: (status: boolean, event: SyntheticEvent) => void;
    size?: "small" | "medium";
    color?: ColorType;
    backgroundColor?: ColorType;
    disableHoverStyle?: boolean;
}
declare const Checkbox: ({ checked, children, dataTest, disabled, expand, onClick, color, backgroundColor: checkboxBackgroundColor, defaultChecked, size, disableHoverStyle, }: CheckboxProps) => React.JSX.Element;
export default Checkbox;
