import * as React from "react";
import { ColorType } from "../../../typings";
export interface BaseCheckProps {
    checked?: boolean;
    disabled?: boolean;
    size?: "small" | "medium";
    color?: ColorType;
    backgroundColor?: ColorType;
    empty?: boolean;
}
declare const BaseCheck: ({ checked, size, backgroundColor, color, empty, disabled }: BaseCheckProps) => React.JSX.Element;
export default BaseCheck;
