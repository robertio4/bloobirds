import * as React from "react";
import { ColorType } from "../../../typings";
export interface ModalCloseIconProps {
    onClick: () => void;
    variant?: "primary" | "gradient";
    color?: ColorType;
    size?: "small" | "medium";
}
declare const ModalCloseIcon: {
    (props: ModalCloseIconProps): React.JSX.Element;
    defaultProps: {
        variant: string;
        color: string;
    };
};
export default ModalCloseIcon;
