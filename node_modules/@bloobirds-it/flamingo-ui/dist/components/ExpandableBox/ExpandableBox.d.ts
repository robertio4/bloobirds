import React from "react";
export type Props = {
    title: string;
    subtitle?: string | React.ReactNode;
    progressPercentage?: number;
    progressText?: string;
    defaultOpened?: boolean;
    nonCollapsable?: boolean;
    children: React.ReactNode;
};
declare function ExpandableBox({ title, subtitle, children, progressPercentage, progressText, defaultOpened, nonCollapsable, ...props }: Props): React.JSX.Element;
declare namespace ExpandableBox {
    var defaultProps: {
        defaultOpened: boolean;
        title: string;
        nonCollapsable: boolean;
    };
}
export default ExpandableBox;
