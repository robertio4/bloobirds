import React from "react";
import { ColorType, IconType } from "../../../typings";
interface SidebarItem {
    icon: IconType;
    children: string;
    selected?: boolean;
    onClick?: () => void;
    color?: ColorType;
    dataTest?: string;
}
declare const SidebarItem: {
    ({ children, icon, onClick, selected, color, dataTest }: SidebarItem): React.JSX.Element;
    defaultProps: {
        color: string;
    };
};
export default SidebarItem;
