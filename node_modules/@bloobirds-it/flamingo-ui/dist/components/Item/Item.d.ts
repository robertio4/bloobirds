import * as React from "react";
import { ReactNode } from "react";
import { BaseItemProps } from "../BaseItem/BaseItem";
export interface ItemProps extends BaseItemProps {
    children: ReactNode;
    right?: any;
}
declare const Item: (Props: ItemProps) => React.JSX.Element;
export default Item;
