import * as React from "react";
import { InputProps } from "../../Input/Input";
declare const DoubleInput: {
    ({ defaultValue, value, type, onChange }: InputProps): React.JSX.Element;
    defaultProps: {
        type: string;
        size: string;
        borderless: boolean;
        defaultValue: {
            start: any;
            end: any;
        };
    };
};
export default DoubleInput;
