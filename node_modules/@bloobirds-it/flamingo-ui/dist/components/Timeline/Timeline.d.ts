import * as React from "react";
export interface TimelineProps {
    children?: any;
    size: "tiny" | "small" | "medium";
}
declare const Timeline: {
    ({ children, size }: TimelineProps): React.JSX.Element;
    defaultProps: {
        size: string;
    };
};
export default Timeline;
