import React from "react";
import { DateValue } from "../ShortTermRelativeDatePicker";
interface TabProps {
    onChange: (value: DateValue) => void;
    untilToday?: boolean;
}
interface PanelProps extends TabProps {
    onCancel: () => void;
}
declare const CustomPanel: ({ onChange, onCancel }: PanelProps) => React.JSX.Element;
export default CustomPanel;
