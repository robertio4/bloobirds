import React from "react";
import { ColorType, IconType } from "../../typings";
export interface Props {
    children: React.ReactNode;
    defaultOpened?: boolean;
    onClick?: () => void;
    icon: IconType;
    iconColor?: ColorType;
    title: string;
    progressPercentage?: number;
    isDisabled?: boolean;
    button?: JSX.Element;
}
declare const ExpandableStep: {
    ({ children, defaultOpened, progressPercentage, onClick, isDisabled, ...props }: Props): React.JSX.Element;
    defaultProps: {
        defaultOpened: boolean;
    };
};
export default ExpandableStep;
