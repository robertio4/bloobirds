import { DateValue } from "./RelativeDatePicker";
import { TFunction } from "i18next";
type DateOption = {
    label: string;
    key?: string;
    value: DateValue;
};
export declare const DEFAULT_OPTIONS_TODAY: (t: TFunction) => DateOption[];
export declare const DEFAULT_OPTIONS: (t: TFunction) => DateOption[];
export declare const DEFAULT_PAST_OPTIONS: (t: TFunction) => DateOption[];
export {};
