import React from "react";
export type Props = {
    title: string;
    tooltipText?: string;
    completed?: boolean;
    isDisabled?: boolean;
    linkYoutube?: string;
    linkHowTo?: string;
    linkTour?: string;
    linkNavigation?: string;
    onClick?: () => void;
    onComplete?: () => void;
    skippable?: boolean;
    skippableText?: string;
};
export type SkippableSectionProps = {
    onComplete?: () => void;
    skippableText?: string;
};
export declare const SkippableSection: ({ skippableText, onComplete }: SkippableSectionProps) => React.JSX.Element;
export declare const SubStep: {
    ({ title, isDisabled, completed, linkYoutube, linkHowTo, linkTour, linkNavigation, onClick, tooltipText, skippable, ...props }: Props): React.JSX.Element;
    defaultProps: {
        completed: boolean;
    };
};
export default SubStep;
