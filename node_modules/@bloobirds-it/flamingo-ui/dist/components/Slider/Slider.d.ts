import React, { ReactNode } from "react";
import { ColorType } from "../../typings";
type Props = {
    color?: {
        primary: ColorType;
        secondary: ColorType;
    };
    activeSlide?: number;
    children: ReactNode;
};
declare const Slider: ({ children, activeSlide, color }: Props) => React.JSX.Element;
export default Slider;
