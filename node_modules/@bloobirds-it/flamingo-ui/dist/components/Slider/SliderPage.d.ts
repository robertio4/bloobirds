import React from "react";
import { ButtonVariant } from "../Button/Button";
import { ColorType } from "../../typings";
export interface SliderPageProps {
    children: React.ReactNode;
    textNextButton?: string;
    position?: number;
    isLast?: boolean;
    color?: ColorType;
    onClick?: (position: number) => void;
    buttonVariant?: ButtonVariant;
    displayButton?: boolean;
}
declare const SliderPage: ({ position, textNextButton, children, onClick, isLast, color, buttonVariant, displayButton, }: SliderPageProps) => React.JSX.Element;
export default SliderPage;
