import React from "react";
import { ColorType } from "../../../typings";
type Props = {
    items: number;
    active: number;
    color?: {
        primary: ColorType;
        secondary: ColorType;
    };
    onClick: (active: number) => void;
};
declare const Dots: React.NamedExoticComponent<Props>;
export default Dots;
