import * as React from "react";
import { ReactElement } from "react";
import { BaseSelectProps } from "../BaseSelect/BaseSelect";
import { CheckItemProps } from "../CheckItem/CheckItem";
export interface MultiSelectProps extends Omit<BaseSelectProps, "children" | "onClick"> {
    children?: ReactElement<CheckItemProps>[];
    onChange?: (value: any) => void;
    onClose?: (value: any) => void;
    sortByChecked?: boolean;
    renderDisplayValue?: (value: Array<string>) => string;
    selectAllOption?: boolean;
    itemsTitle?: string;
    allOptionLabel?: string;
}
declare const MultiSelect: {
    (Props: MultiSelectProps): React.JSX.Element;
    defaultProps: {
        defaultValue: any[];
        size: string;
        sortByChecked: boolean;
    };
};
export default MultiSelect;
