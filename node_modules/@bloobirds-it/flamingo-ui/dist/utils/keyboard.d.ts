import { KeyboardEvent } from "react";
export declare const isUndoEvent: <T extends Element>(event: KeyboardEvent<T>) => boolean;
export declare const isRedoEvent: <T extends Element>(event: KeyboardEvent<T>) => boolean;
